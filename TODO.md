# TODO: Integrate useApplication Hook

- [x] Make the `request` method in `src/services/api.ts` public to allow generic API calls.
- [x] Create `hooks/useApplication.ts` with the adjusted hook code, updating imports and API calls to use `apiService.request`.
- [x] Install react-router-dom dependency.
- [x] Fix API call signatures to match the ApiService.request method (using RequestInit options).
